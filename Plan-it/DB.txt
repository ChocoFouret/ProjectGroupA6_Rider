-- DBCC CHECKIDENT (table, RESEED, 0)

USE gamer;

DROP DATABASE GroupeA6;
CREATE DATABASE GroupeA6;

USE GroupeA6;

CREATE TABLE Functions
(
    title VARCHAR(50) NOT NULL PRIMARY KEY
)

CREATE TABLE Accounts
(
    idAccount INT IDENTITY NOT NULL PRIMARY KEY,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(200) NOT NULL,
    lastName VARCHAR(50) NOT NULL,
    firstName VARCHAR(100) NOT NULL,
    street VARCHAR(100) NOT NULL,
    number VARCHAR(10) NOT NULL,
    postCode INT NOT NULL,
	city VARCHAR(30) NOT NULL,
    isChief bit NOT NULL,
    pictureURL VARCHAR(100) NOT NULL,
    title VARCHAR(50) NOT NULL,
    FOREIGN KEY(title) REFERENCES Functions(title)
)

CREATE TABLE Schedules
(
    idSchedule   INT IDENTITY NOT NULL PRIMARY KEY,
    DATESchedule DATE         NOT NULL,
    startHour    TIME         NOT NULL,
    endHour      TIME         NOT NULL,
    breakTime    TIME         NOT NULL,
    idAccount    INT          NOT NULL,
    FOREIGN KEY(idSchedule) REFERENCES Accounts(idAccount)
);

CREATE TABLE Holidays
(
    idHolidays   INT IDENTITY NOT NULL PRIMARY KEY,
    DATEHolidays DATE         NOT NULL,
    isAccepted   BIT          NOT NULL,
    comments     VARCHAR(200),
    idAccount INT NOT NULL,
    FOREIGN KEY(idHolidays) REFERENCES Accounts(idAccount)
);

CREATE TABLE Absents
(
    idAbsent INT IDENTITY NOT NULL PRIMARY KEY,
    DATEAbsent DATE NOT NULL,
    isJustified BIT NOT NULL,
    comments VARCHAR(200) NOT NULL,
    idAccount INT NOT NULL,
    FOREIGN KEY(idAbsent) REFERENCES Accounts(idAccount)
)

INSERT INTO dbo.Functions(title)
	VALUES 
	('Employee'),
	('Director'),
	('Administrator');

-- Password = 12345
INSERT INTO Accounts
           (email, password, lastName, firstName, street, number, postCode, city, isChief, pictureURL, title)
     VALUES
           ('user1@gmail.com', '1000:lAwhECG/vjIu5XaM8/6Vnofas95NKCat:/SZEXfEa7RE6wrF1zFxJSafHSzs=', 'Nom 1', 'Prenom 1', 'RUE 1', 'NUM 1', 1, 'VILLE 1', 1, 'image', 'Employee'),
		   ('user2@gmail.com', '1000:lAwhECG/vjIu5XaM8/6Vnofas95NKCat:/SZEXfEa7RE6wrF1zFxJSafHSzs=', 'Nom 2', 'Prenom 2', 'RUE 2', 'NUM 2', 2, 'VILLE 2', 1, 'image', 'Employee'),
		   ('user3@outlook.fr', '1000:lAwhECG/vjIu5XaM8/6Vnofas95NKCat:/SZEXfEa7RE6wrF1zFxJSafHSzs=', 'Nom 3', 'Prenom 3', 'RUE 3', 'NUM 3', 3, 'VILLE 3', 1, 'image', 'Employee'),
           ('director', '1000:lAwhECG/vjIu5XaM8/6Vnofas95NKCat:/SZEXfEa7RE6wrF1zFxJSafHSzs=', 'Directeur', 'Directeur', 'RUE', 'NUM', 2, 'VILLE', 1, 'image', 'Director'),
           ('employee', '1000:lAwhECG/vjIu5XaM8/6Vnofas95NKCat:/SZEXfEa7RE6wrF1zFxJSafHSzs=', 'Employé', 'Employé', 'RUE', 'NUM', 3, 'VILLE', 1, 'image', 'Employee');
